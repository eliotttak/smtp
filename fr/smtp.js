let reboot = true
while (reboot) {
    reboot = false
    //try {
        const nodemailer = require("http2")
        console.log(nodemailer)
    //}
    //catch (error){
      //  console.log(error)
    //}
}